
client_id=${client-id}
client_secret=${secret}
denomination=${denomination}
gameInstanceId=${gameInstanceId}
baseUrl=${baseUrl}
clientBackgroundImage=${clientBackgroundImage}
gameUrl=${gameUrl}
clientLedgerId=${clientLedgerId}
loyai_baseUrl=${loyai_baseUrl}
FT_applicationId=${FT_applicationId}

billingUrl=${BILLING_URL}
invoiceUrl=${INVOICE_URL}
userUrl=${USER_URL}
paymentUrl=${PAYMENT_URL}
invoiceValidity=${INVOICE_VALIDITY}
invoiceType=${INVOICE_TYPE}
productUrl=${PRODUCT_URL}
eventUrl=${EVENT_URL}
amountTobeWonPercentage =${AMOUNT_TO_BE_WON_PERCENTAGE}
spring.datasource.url=${db_url}
spring.datasource.username=${db_username}
spring.datasource.password=${db_password}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto = update
spring.jpa.database=postgresql
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect



